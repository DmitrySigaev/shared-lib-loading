This is example of shared library loading on different platform like Windows, Linux
__Execution__

Running on Linux, Windows and MacOS:
`cmake . && make`. The executable is then available in `Core/build/`.
